meta {
  name: file upload
  type: http
  seq: 16
}

post {
  url: {{base_url}}/api/v0/sps/collections/{{collection_id}}/items/file?separator=;
  body: multipartForm
  auth: bearer
}

query {
  separator: ;
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  file: @file(/home/savyaznikov/Загрузки/materials.csv)
}

vars:pre-request {
  collection_id: 1
}
