meta {
  name: create_collection
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/v0/sps/collections
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "name": "test"
  }
}
