meta {
  name: delete_collection
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/api/v0/sps/collections/{{collection_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  collection_id: 2
}
