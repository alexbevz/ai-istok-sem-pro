meta {
  name: get all collections
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/v0/sps/collections/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  //let jsonData = res.body;
  //let id = jsonData[0].id;
  //bru.setEnvVar("collection_id", id);
}
